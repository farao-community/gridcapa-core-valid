spring:
  rabbitmq:
    host: localhost-test
    port: 1234
    username: guest-test
    password: guest-test

core-valid-server:
  requests:
    queue-name: core-valid-test
  responses:
    exchange: core-valid-response-test
    expiration: 60000
  minio:
    access:
      name: minioadmin
      secret: minioadmin
    url: http://localhost:9000
    bucket: core-validation
    base-path: core-valid/
  security:
    whitelist:
      - file:/
      - http://${MINIO_HOST:localhost}:${MINIO_PORT:9000}